# Dot File Manager

Simple python dotfile manager.

Automatically creates symlinks between your dotfile directory and your config
directory.

## Setup

Install [uv](https://docs.astral.sh/uv/), then: `make install`

